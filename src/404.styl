@import "/src/base/const"

html
    background $dark
    color $light
    font $fz/$lh $mono
    text-smooth()

body
    max-width 82ch
    margin 0 auto
    padding ($lh)em 1ch

#url
    margin 0 0 ($lh)em
    font-weight bold
    overflow hidden
    text-overflow ellipsis
